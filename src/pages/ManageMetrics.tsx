import React from 'react'

import { ManageMetrics as ManageMetricsFeature } from '../features/ManageMetrics/ManageMetrics'

export const ManageMetrics: React.FC = () => {
  return <ManageMetricsFeature />
}
